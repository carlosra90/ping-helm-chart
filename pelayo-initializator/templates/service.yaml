apiVersion: v1
kind: Service
metadata:
  name: {{ include "pelayo-initializator.fullname" . }}
  labels:
    {{- include "pelayo-initializator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pelayo-initializator.selectorLabels" . | nindent 4 }}
