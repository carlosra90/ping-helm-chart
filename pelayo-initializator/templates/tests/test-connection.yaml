apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pelayo-initializator.fullname" . }}-test-connection"
  labels:
    {{- include "pelayo-initializator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pelayo-initializator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
